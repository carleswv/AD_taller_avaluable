---
title: "Taller evaluado de repaso para el Primer Parcial"
subtitle: "20582- Análisis de Datos para el GMAT"
date: today
author: "Carles Westendorf Vidal"
format:
  html:
    theme: lumen
    toc: true
    toc-depth: 3
Rendering:
    embed-resources: true
---

Quiero ver que en la adolescencia el tiempo dedicado al móvil es mayor que entre adultos. Para ello, nos interesan las variables siguientes: 


# RESUMEN
Estudiaremos la como varía el tiempo dedicado a las pantallas entre distintos rangos de edad.


## OBJETIVOS
Ver que la edad no es un factor significativo para consumir más redes sociales


### METODOLOGÍA



## RESULTADOS



### IMPLICACIONES



# PLANTEAMIENTO
Cremos unos datos con los que trabajar a partir de una normal multivariante de media $\mu = (0.7,0.66,0.51,6.5)$ y matriz de covarianzas:


```{r}
library(MASS)

media <- c(0.7, 0.66, 0.51, 6.5)  # Medias de consumo_pantalla (horas/dia), consumo_aplicaciones (horas/dia), consumo_redes (horas dia), horas_ocupado (horas/semana)
covarianza <- matrix(c(0.3, 0.08, 0.05, 0.05,
                       0.09, 0.28, 0.03, 0.01,
                       0.08, 0.005, 0.32, 0.01,
                       0.8, 0.78, 0.79, 3), 
                     nrow = 4, ncol = 4)


set.seed(23)
datos_numericos <- mvrnorm(150, mu = media, Sigma = covarianza)

# Limitar y ajustar valores de promedio_calificaciones a escala 1-10
datos_numericos[,4] <- pmin(pmax(round(datos_numericos[,4], 2), 0), 60)

# Tabla 1 con los datos 
tiempo_libre <- data.frame(
  consumo_pantalla = round(datos_numericos[,1], 2),
  consumo_aplicaciones = round(datos_numericos[,2], 2),
  consumo_redes = round(datos_numericos[,3]),
  horas_ocupado = datos_numericos[,4],
  tipo_escuela = sample(c("Pública", "Privada"), 150, replace = TRUE),
  nivel_interes = ordered(sample(1:5, 150, replace = TRUE), labels = c("Muy Bajo", "Bajo", "Medio", "Alto", "Muy Alto")),
  habito_lectura = ordered(sample(1:5, 150, replace = TRUE), labels = c("Nada", "Poco", "Regular", "Frecuente", "Muy Frecuente"))
)
```




# ANÁLISIS DESCRIPTIVO



# PRUEBAS ESTADÍSTICAS



# CONCLUSIONES



# BIBLIOGRAFÍA



























